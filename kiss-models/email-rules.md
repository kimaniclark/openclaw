# Kiss Models — Email Rules

## Approval Process
1. **NO email is sent without Victoria's explicit approval.** Always show a draft first and wait for "approved" / "it's approved" before sending. No exceptions until further notice.

## Sender Identity
2. **Default sender name: Maria (Maria Mara, Head Booker)** — unless the incoming email is specifically addressed to Victoria (e.g. "Hi Victoria"), in which case respond as Victoria.
3. If unsure which name to use → ask Victoria before sending.

## Sign-off
4. Sign-off options:
   - **"Best,\nVictoria"** — when responding as Victoria
   - **"Best,\nMaria Mara\nHead Booker"** — when responding as Maria
5. **Do NOT add any other sign-off** (no "KiSS Models", no extra text). Gmail signature handles the rest.

## Draft Presentation
7. When presenting a draft, always indicate whether it's a **reply** (to an existing thread) or a **new email**.
8. For new emails, always include the **subject line** in the draft. (Replies inherit the existing subject.)

## Signature
6. Gmail's signature does NOT auto-append when sending via the API. I must include it manually in every email.
   - Signature HTML is saved at `kiss-models/email-signature.html`
   - Append it after the sign-off in every outgoing email (as HTML)
   - Send emails as `text/html` (not plain text) so the signature renders correctly

## Subject Lines
9. **No special characters or Unicode symbols** in subject lines — no arrows (→), em dashes (—), emojis, or non-ASCII characters. Stick to plain text only. These can render as garbled symbols (e.g. ÃÂ±Â) on some email clients due to encoding issues.

---

*Established: July 21, 2026 — per Kimani*
